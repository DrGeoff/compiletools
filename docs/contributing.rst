Contributing
============

The contents below are taken verbatim from ``README.coders`` at the
project root. It uses shell-style comment markers, not RST, so it is
embedded as a literal block.

.. literalinclude:: ../README.coders
   :language: text
