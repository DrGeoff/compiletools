# Header units are a C++20 feature; both gcc and clang accept
# `import <header>;` at -std=c++20 with the appropriate module
# enablement flags (auto-injected by compiletools). This example uses
# the default variant's standard; the cross-backend matrix overrides
# the toolchain axis to exercise both gcc and clang.
CXXFLAGS = -std=c++20
