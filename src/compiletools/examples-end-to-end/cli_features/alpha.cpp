// ct-exemarker: one of three executables in this sample so the
// build-only-changed and per-target build paths have something to bite on.
#include <cstdio>

int main()
{
    std::printf("alpha\n");
    return 0;
}
