# Sample-local config so the moonlander sample is self-contained. The
# compiler-specific module flag (gcc: -fmodules-ts; clang: none extra at this
# std) is auto-injected for any TU in a module dep edge -- the user only opts
# into the language standard here.
CXXFLAGS = -std=c++26
