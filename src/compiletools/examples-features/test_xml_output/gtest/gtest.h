// Stub for compiletools' end-to-end test of --test-xml-dir framework
// detection. Empty: the test fixture handles --gtest_output=xml:PATH
// itself rather than depending on a real GoogleTest install. Detection
// triggers off the include path "gtest/gtest.h" appearing in the
// transitive header set, not off any symbol exported here.
