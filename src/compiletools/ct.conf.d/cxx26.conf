# Language standard axis: C++26. Requires gcc >= 14 or clang >= 18.
# (Older compilers spell this -std=c++2c — if you're on those, set
# CXXFLAGS in your project ct.conf or use a project-level cxx2c.conf.)
append-CXXFLAGS = -std=c++26
