# Compile-time profiling axis (clang's `-ftime-trace`).
# Emits per-TU .json trace files next to each .o; view in chrome://tracing
# or speedscope.app to find slow template instantiations / parse stages.
append-CFLAGS   = -ftime-trace
append-CXXFLAGS = -ftime-trace
