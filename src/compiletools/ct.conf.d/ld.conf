# Linker axis: GNU bfd ld (the historical default).
# Explicit `-fuse-ld=bfd` so the choice is pinned in the cache key and
# reproducible across systems where mold/gold may be the system default.
append-LDFLAGS = -fuse-ld=bfd
