# Linker axis: mold (modern parallel linker by Rui Ueyama).
# Significantly faster than bfd ld and gold on large C++ projects.
# Compatible with gcc (>= 12.1) and clang (>= 14) via -fuse-ld=mold.
append-LDFLAGS = -fuse-ld=mold
