# Split-DWARF debug-info axis: faster link, smaller binaries.
# Debug info lands in per-TU .dwo files instead of the executable. The
# --gdb-index link flag lets gdb load symbols on demand.
append-CFLAGS   = -gsplit-dwarf
append-CXXFLAGS = -gsplit-dwarf
append-LDFLAGS  = -Wl,--gdb-index
